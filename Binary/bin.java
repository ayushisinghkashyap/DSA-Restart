class Solution {
    public int majorityElement(int[] nums) {
        int count =1;
        int n = 
        Arrays.sort(nums);
        for(int i=1;i<nums.length;i++){
            if(nums[i] == nums[i-1]){
                count++;
            }
            if(count>)
        }
    }
}
